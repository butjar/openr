# This file was @generated by getdeps.py

name: linux

on:
  push:
    branches:
    - master
    - Fix-debian-system-builder
  pull_request:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Generate debain install script
      run: sudo python3 build/debian_system_builder/debian_system_builder.py > build_openr_debian.sh && cat build_openr_debian.sh
    - name: Make debian install script executable
      run: chmod +x build_openr_debian.sh
    - name: Run debian install script
      run: sudo ./build_openr_debian.sh
